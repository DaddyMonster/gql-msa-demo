import { graphqlServiceLms } from './graphql-service-lms';

describe('graphqlServiceLms', () => {
  it('should work', () => {
    expect(graphqlServiceLms()).toEqual('graphql-service-lms');
  });
});
