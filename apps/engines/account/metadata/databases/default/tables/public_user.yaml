table:
  schema: public
  name: user
array_relationships:
  - name: tickets
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: ticket
